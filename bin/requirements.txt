# Python dependencies for llm-wiki tooling.
# Install via:  pip3 install --user -r bin/requirements.txt
# (or run bin/setup.sh which does this for you)

pymupdf4llm>=0.0.27   # PDF → raw markdown for bin/transcribe.py
pytest>=8.0           # unit tests: python3 -m pytest bin/tests/
umap-learn>=0.5       # UMAP projection for bin/knowledge_map.py
networkx>=3.0         # Louvain communities + bridge/sparse analysis for bin/knowledge_map.py
# Visualization itself runs on Cytoscape.js, vendored under bin/vendor/ —
# no Python rendering library required.
